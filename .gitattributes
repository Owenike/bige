# Normalize line endings across platforms.
* text=auto eol=lf

# Keep attributes file itself LF so it doesn't flap.
.gitattributes text eol=lf

# Windows scripts should stay CRLF.
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf
